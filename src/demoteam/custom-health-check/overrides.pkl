amends "..."

//App-based overrides

// Change the path and the port for the health checks

includeHealthCheck = true

probePath = "/"
probePort = 80

images {
  [module.app] {
    image = "nginxinc/nginx-unprivileged:alpine3.22-perl"
  }
}