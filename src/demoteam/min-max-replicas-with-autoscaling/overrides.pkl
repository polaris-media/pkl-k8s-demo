amends "..."

// Sometimes we need to override and set a custom min max replicas


horizontalPodAutoscalers {
    [module.app] {
        spec {
        minReplicas = 2 // minimum 1 (Default is 2 in production)
        maxReplicas = 20 // limit 1 (Default is 19 in production)
        }
    }
}